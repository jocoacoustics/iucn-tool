/*
 * IUCN Tool — configuración opcional.
 *
 * La edición principal NO necesita backend remoto:
 * GitHub Pages -> Jocotoco IUCN Connector (extensión local) -> IUCN API v4.
 *
 * proxyBase queda vacío deliberadamente. Se conserva solo para facilitar
 * futuros despliegues alternativos sin reescribir iucn-core.js.
 */
window.IUCN_TOOL_CONFIG = Object.freeze({ proxyBase: "" });
